import 'package:flutter/cupertino.dart';

mixin Styles {
  static const productItemName = TextStyle(fontSize: 18);

  static const productPriceStyle = TextStyle(
    color: Color(0xFF848484),
    fontSize: 14,
    fontWeight: .w300,
  );
}
