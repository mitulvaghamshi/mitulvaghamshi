name: apis
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.0.0
  flutter_html: ^3.0.0-beta.2
  cupertino_icons: ^1.0.4

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/gita.json
  - assets/wiki.json
  - assets/users.json
  - assets/realestate.json
  - assets/marsphotos.json
