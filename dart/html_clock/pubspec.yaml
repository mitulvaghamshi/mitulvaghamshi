name: html_clock
description: An absolute bare-bones web app.

version: 1.0.0

environment:
  sdk: ^3.11.4

dependencies:
  web: ^1.1.1

dev_dependencies:
  build_runner: ^2.4.15
  build_web_compilers: ^4.1.6
  lints: ^6.1.0
