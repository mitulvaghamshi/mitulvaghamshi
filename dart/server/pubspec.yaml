name: server
description: A server app using the shelf package.
version: 1.0.0

environment:
  sdk: ^3.10.4

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_static: ^1.1.3
  sqlite3: ^3.1.1

dev_dependencies:
  http: ^1.1.0
  lints: ^6.0.0
  test: ^1.24.0
